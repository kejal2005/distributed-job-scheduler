PORT=4000
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=job_scheduler
JWT_SECRET=change-this-in-production

# Worker service (set these when running `npm run worker`)
API_BASE=http://localhost:4000/api
PROJECT_ID=
PROJECT_API_KEY=
WORKER_QUEUES=
WORKER_CONCURRENCY=5
POLL_INTERVAL_MS=1000
HEARTBEAT_INTERVAL_MS=5000

# Scheduler service
SCHEDULER_TICK_MS=5000
